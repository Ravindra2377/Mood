# Example environment variables for local development
# Copy this file to `.env` and adjust values for your environment.

# Database
DATABASE_URL=sqlite:///./mh.db

# Optional: AWS KMS key id for envelope encryption (recommended for production)
#KMS_KEY_ID=arn:aws:kms:us-east-1:123456789012:key/abcd-ef01-2345-6789

# Optional: local Fernet key used when KMS is not configured. Use `Fernet.generate_key()` to create one.
#DATA_ENCRYPTION_KEY=

# Optional: S3 bucket for analytics exports
#ANALYTICS_S3_BUCKET=my-analytics-bucket

# Optional: Segment write key for analytics pipeline
#SEGMENT_WRITE_KEY=

# AWS credentials (for local testing with AWS SDKs). In production, prefer IAM roles.
#AWS_ACCESS_KEY_ID=
#AWS_SECRET_ACCESS_KEY=
#AWS_REGION=us-east-1

# App specific
#DATA_RETENTION_DAYS=365
# Example environment variables for development
DATABASE_URL=sqlite:///./mh.db
REDIS_URL=redis://localhost:6379/0
SECRET_KEY=dev-secret

# Example Postgres (used by docker-compose)
# DATABASE_URL=postgresql+psycopg2://mh_user:mh_pass@localhost:5432/mh_db

# To run Postgres + app locally using docker-compose:
# docker compose up --build
ACCESS_TOKEN_EXPIRE_MINUTES=60
