<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pastel gradient background -->
    <background android:drawable="@drawable/ic_launcher_background_gradient" />
    <!-- Foreground image in drawable(-nodpi)/launcher_foreground.* -->
    <foreground android:drawable="@drawable/launcher_foreground" />
    <!-- Temporary monochrome using the same foreground until a vector is provided -->
    <monochrome android:drawable="@drawable/launcher_foreground" />
</adaptive-icon>
